# Enable strict peer dependencies
strict-peer-dependencies=false

# Use hoisted node_modules layout for Vite compatibility (avoids "Cannot find module dist.js" with isolated symlinks)
node-linker=hoisted

# Auto install peers
auto-install-peers=true
